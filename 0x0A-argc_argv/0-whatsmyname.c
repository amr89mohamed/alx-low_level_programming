#include <stdio.h>
/*
 * this funcation
 * where is the main fancation
 * takes two arguments
 * the first is argc the number of argumants
 * the Second is the argumants ve
 */int main(int argc, char *argv[])
{
if (argc > 0)
{
printf("%s\n", argv[0]);
}
return (0);
}
