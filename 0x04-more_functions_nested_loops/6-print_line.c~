#include "main.h"
/*
 * this a funcation decleration
 * which print multible _
 * in one line 
 */void print_line(int n)
{
int i = 0;
while (i < n)
{
_putchar('_');
i++;
}
{
if (n <= 0)
_putchar('\n');
}
}
