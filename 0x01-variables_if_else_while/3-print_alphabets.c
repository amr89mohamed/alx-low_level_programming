#include <stdio.h>
/*
*thid is the mainfuncation */int main(void)
{
char ch;
char ch1;
{
for (char ch = 'a' ; ch <= 'z' ; ++ch)
putchar (ch);
}
{
for (char ch1 = 'A' ; ch1 <= 'Z' ; ch1++)
putchar (ch1);
}
putchar ('\n');
return (0);
}
