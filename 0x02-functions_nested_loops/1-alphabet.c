#include "main.h"
/*
* this the main funcation*/int main(void)
{
void print_alphabet(void);
char alphabet = 'a';
{
print_alphabet();
return (0);
}
{
for (alphabet = 'a' ; alphabet <= 'z' ; alphabet++)
_putchar(alphabet);
_putchar('\n');
}
