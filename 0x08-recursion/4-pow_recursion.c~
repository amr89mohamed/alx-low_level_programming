#include "main.h"
/*
 * this funcation
 * will return
 * the power rised of n by y
*/int _pow_recursion(int x, int y)
{
if (y < 0)
{
return (-1);
}
if (y == 0)
{
return (0);
}
return (x * _pow_recursion(x, y - 1));
}
